#Для локального запуска тестов комментируем окружение для Docker
#Для запуска в Docker комментируем локальное окружение

# Для локального запуска (Без Docker)
gateway.url=http://localhost:8084
environment=local
servicea.endpoint=/serviceA/hello
serviceb.endpoint=/serviceB/hello
request.timeout=7000
build.version=3.3.0
debug=true

# Для запуска в Docker
#gateway.url=http://gateway:8080
#environment=docker
#servicea.endpoint=/serviceA/hello
#serviceb.endpoint=/serviceB/hello
#request.timeout=7000
#build.version=3.3.0
#debug=true




